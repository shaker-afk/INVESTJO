/**
 * app/(tabs)/index.tsx
 * Entry point for the first tab — mounts the Discovery Dashboard screen.
 */
import DiscoveryDashboard from '../../src/screens/DiscoveryDashboard';

export default DiscoveryDashboard;
