/**
 * app/listing/[id].tsx
 * Dynamic route for the Listing Detail screen.
 * Expo Router resolves the `id` segment from the URL.
 */
import ListingDetailScreen from '../../src/screens/ListingDetailScreen';
export default ListingDetailScreen;
